# 1.1.0 发布前复核

本次发布保留已认可的 178 行 skill。与 `e72ae34` 相比，运行文件只将 `metadata.version` 从 1.0.0 改为 1.1.0；71 行实验版没有成为默认版本。新增中文、混排与 Issue/PR 编辑能力的来源和历轮取舍见[改进记录](../../../docs/RSI.md)。

## 行为回归

固定输入是仓库现有的 [28 个回归场景](../../cases.json)，运行指导取自 `f13ad1cda9b7ab958854e5351eb1cf2dba6db562`，SHA-256 为 `9c1be9fc3985c3e04422cd6f9808405ccf3fbdf541af72417008660908f2554c`。模型为 `gpt-6-astra`，推理设置 `max`。

- 27 个文本场景各生成一次，独立临时会话，并发 3、超时 240 秒；不向改稿模型提供验收要点。完整配置、输出及用量见 [text/](text/)。
- `zh-11-file-protection` 在独立可写目录内实际保存并修改 `guide.md`，网络关闭；保留[成稿](file-case/guide.md)、[动作记录](file-case/result.json)和[保护检查](file-case/assessment.json)。公开动作记录只把临时绝对路径替换为 `$CASE_DIR`，成稿未改。
- 共 28 次模型调用，没有执行失败或重试。维护会话逐项核对用户要求、原始材料与输出，未发现实质信息错误或明确修改范围违背。代码、元数据、路径与链接的文件保护检查通过，样例中的命令没有执行。

逐例审阅见 [assessment.json](assessment.json)。这是维护会话的非盲复核，不是外部中文编辑的独立评测，也不是新保留集。`zh-14` 仍保留“持续投入有了成效”的概括评价；用户同时要求每段信息都留下，因此未将此评价的保留判为信息失败，但不能据此声称所有模板痕迹已经消除。

## 包装与安装准备

发布前通过仓库校验、29 项维护脚本单元测试、Claude marketplace 校验与严格插件校验。Skills CLI 1.5.25 在临时项目中发现一个 skill，并成功安装到 Codex / Claude Code 对应路径，逐字核对正文和许可。

精简附件只包含 `SKILL.md`、`LICENSE`、`THIRD_PARTY_NOTICES.md` 和 `agents/openai.yaml`。候选附件已经与固定提交逐文件核对；正式附件须从最终 tag 构建，发布后再从公开下载地址核对。此处不把尚未发生的远端安装或用户本机安装记成通过。

运行正文仍然自包含。上述模型输出、脚本和研究记录用于维护，不进入精简安装包；本轮没有恢复 RSI 定时任务。
