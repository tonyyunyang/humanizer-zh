[
  {
    "id": "cal-pair-missing-evidence",
    "request": "你是中文编辑评审。根据原文、用户要求、上下文、可选样文和验收要点，比较匿名改稿 A、B。\n验收要点是待核对的检查提示，不能给用户任务追加要求。判断遗漏是否实质性，要依据用户实际要完成的表达；从材料整理回复不等于逐项复述材料。错误必须回指原始任务和材料，说明它怎样妨碍本次交付，不能仅因缺少某个检查项就判失败。用户直接提出的问题必须回答。\n先用一句话说明这一次具体想完成的表达：读者需要理解、感受或据此做什么。不要用文体名称代替这个判断，也不要擅自给原文增加情绪或立场。\n检查事实、数字、角色与归属、否定、条件、范围、时序、完成状态、作者实质立场、引文和精确字符，以及用户明确的修改边界。语气和表达方式可以按用户意图调整，等义改写不算错误。遗漏或改变实质信息、违反明确范围、增加无法由材料支持的确定事实，都使信息门槛为 fail；即使无法知道新增断言在现实中是否为真，也不能只记为 uncertain。只有材料或表达本身有歧义，无法确定是否发生上述错误时才用 uncertain；否则为 pass。纯审美取舍不记成硬错误。\n在信息与范围成立后，按这一次的目的比较：重点、解释程度和力度是否贴切；措辞、语序、承接、省略和停顿是否自然；有没有残留或新加的模板式铺垫、抽象空转、机械排比、强行收尾或无依据的姿态。\ntemplate_issues 只记录改稿中能定位且在此处没有作用的模板问题，引用原文和改稿说明原因；不能因为一个词、一个转折、三项列举或一处修辞就贴标签。没有这类问题时用空数组，不推测作者是否是 AI。\n平直可以很好，不默认奖励更短、口语化、更明确或更多修辞。需要表现力时，检查改稿是否通过已有意思的措辞、句序、节奏或呼应达到效果；不能仅因保留原文就算完成改进。原文已经适合本次需要时，少改或不改也可成立。不要把有作用的含蓄补成解释，也不要为平直文字硬添意味。\nreason 要把偏好联系到具体表达目的、自然中文和去模板效果，并引用相关差异。两版各有取舍或收益不清时允许 tie；材料不足时选 uncertain。只按给定 JSON schema 输出，不猜版本身份。",
    "input": "{\n  \"request\": \"按项目模板整理 PR 描述，保留 Changes 和 Validation 两个小节。\",\n  \"input\": \"本次提供的全部改动记录：将说明中的示例路径从 `examples/basic` 更正为 `examples/quickstart`。\",\n  \"A\": \"## Changes\\n\\n将示例路径从 `examples/basic` 更正为 `examples/quickstart`。\\n\\n## Validation\\n\\n未提供验证信息。\",\n  \"B\": \"## Changes\\n\\n将示例路径从 `examples/basic` 更正为 `examples/quickstart`。\\n\\n## Validation\\n\\n未运行测试。\"\n}"
  },
  {
    "id": "cal-pair-task-over-checklist",
    "request": "你是中文编辑评审。根据原文、用户要求、上下文、可选样文和验收要点，比较匿名改稿 A、B。\n验收要点是待核对的检查提示，不能给用户任务追加要求。判断遗漏是否实质性，要依据用户实际要完成的表达；从材料整理回复不等于逐项复述材料。错误必须回指原始任务和材料，说明它怎样妨碍本次交付，不能仅因缺少某个检查项就判失败。用户直接提出的问题必须回答。\n先用一句话说明这一次具体想完成的表达：读者需要理解、感受或据此做什么。不要用文体名称代替这个判断，也不要擅自给原文增加情绪或立场。\n检查事实、数字、角色与归属、否定、条件、范围、时序、完成状态、作者实质立场、引文和精确字符，以及用户明确的修改边界。语气和表达方式可以按用户意图调整，等义改写不算错误。遗漏或改变实质信息、违反明确范围、增加无法由材料支持的确定事实，都使信息门槛为 fail；即使无法知道新增断言在现实中是否为真，也不能只记为 uncertain。只有材料或表达本身有歧义，无法确定是否发生上述错误时才用 uncertain；否则为 pass。纯审美取舍不记成硬错误。\n在信息与范围成立后，按这一次的目的比较：重点、解释程度和力度是否贴切；措辞、语序、承接、省略和停顿是否自然；有没有残留或新加的模板式铺垫、抽象空转、机械排比、强行收尾或无依据的姿态。\ntemplate_issues 只记录改稿中能定位且在此处没有作用的模板问题，引用原文和改稿说明原因；不能因为一个词、一个转折、三项列举或一处修辞就贴标签。没有这类问题时用空数组，不推测作者是否是 AI。\n平直可以很好，不默认奖励更短、口语化、更明确或更多修辞。需要表现力时，检查改稿是否通过已有意思的措辞、句序、节奏或呼应达到效果；不能仅因保留原文就算完成改进。原文已经适合本次需要时，少改或不改也可成立。不要把有作用的含蓄补成解释，也不要为平直文字硬添意味。\nreason 要把偏好联系到具体表达目的、自然中文和去模板效果，并引用相关差异。两版各有取舍或收益不清时允许 tie；材料不足时选 uncertain。只按给定 JSON schema 输出，不猜版本身份。",
    "input": "{\n  \"request\": \"下面是两条 review 和我手头的实现材料。帮我写两条可以直接回复评审者的中文回复，分别编号，技术标识保持原样。\",\n  \"input\": \"PR 背景：侧栏偏好设置存储在 panel:prefs。这个值如果不是合法 JSON，打开设置面板会抛错。\\n\\n评审 1：\\n“解析失败以后怎么处理旧值？别把整个 localStorage 清了。正常配置和以后加的字段还能保留吗？”\\n\\n评审 2：\\n“这次是否顺便加 schema version 和 migration？另外请确认整套测试都绿了再合。”\\n\\n三天前的实现笔记：\\n“parse 报错就删除 panel:prefs，写入一份默认值；一起加 v2 migration。”\\n\\n当前准备提交的完整读取函数：\\n```ts\\nfunction readPrefs(storage: Storage) {\\n  const raw = storage.getItem('panel:prefs');\\n  if (raw === null) return { ...DEFAULT_PREFS };\\n  try {\\n    const parsed = JSON.parse(raw);\\n    return isPlainObject(parsed)\\n      ? { ...DEFAULT_PREFS, ...parsed }\\n      : { ...DEFAULT_PREFS };\\n  } catch {\\n    return { ...DEFAULT_PREFS };\\n  }\\n}\\n```\\n此处 isPlainObject 仅判定非 null 的普通对象，不校验各字段值。这次改动没有其他 storage 读写，也没有 schema version 字段。\\n\\n当前验证记录：\\n```text\\nprefs.test.ts: 8 passed\\n覆盖：无存储值、坏 JSON、数组、null、缺省字段合并、已有字段覆盖默认值、未知字段随对象保留、默认对象不被修改。\\nfull suite: 79 passed, 1 failed\\nfailed: table-layout.test.ts / narrow table snapshot\\n```\\n\\n我自己的补充：快照失败还没复查。我猜可能和测试字体有关，但没有证据。migration 没有定排期，这次回复先不要替我承诺下一版会做。\",\n  \"expectations\": [\n    \"输出两条分别编号、可直接用于回复评审者的中文回复，逐条回应对应问题。\",\n    \"以当前函数为准说明读不到值、解析失败或结果不是普通对象时返回一份默认配置，不沿用早期笔记中删除旧值、写入默认值的做法。\",\n    \"说明当前修改不清空 localStorage，也不修改已存储的 panel:prefs；坏值仍留在存储中，后续读取仍会遇到它。\",\n    \"准确说明普通对象与默认值合并、已有值覆盖默认值、未知字段在返回对象中保留；不能声称所有字段类型或语义已经校验。\",\n    \"明确这次没有 schema version 或 migration，不擅自承诺已排期、下一版本一定实现或其他材料中没有的后续行动。\",\n    \"如报告测试，区分 prefs.test.ts 的八个通过与整套七十九个通过、一个失败，不能回复整套已绿或已经满足评审者的合入条件。\",\n    \"保留 table-layout.test.ts / narrow table snapshot 失败尚待复查的状态；不能把字体猜测写成确定原因或已经证明与本次改动无关。\",\n    \"保留 panel:prefs、localStorage、schema version、migration 等技术标识的含义，不添加不存在的提交、测试日志链接或部署记录。\"\n  ],\n  \"A\": \"1. 旧值会保留。当前实现只读取 `panel:prefs`，解析失败时返回 `DEFAULT_PREFS` 的副本，不删除或覆写旧值，也不会清空 `localStorage`。解析结果是普通对象时，缺省字段用默认值补齐，已有字段覆盖默认值，未知字段也会保留。数组、`null` 等非普通对象会回退到默认值。这里的 `isPlainObject` 只判断是否为非 `null` 的普通对象，不校验各字段值。\\n\\n2. 这次没有加入 schema version 或 migration，migration 目前也没有排期。整套测试还没全绿：`prefs.test.ts: 8 passed`，但 `full suite: 79 passed, 1 failed`，失败项是 `table-layout.test.ts / narrow table snapshot`。快照失败还没复查，可能与测试字体有关只是我的猜测，目前没有证据。合入前还需要复查这项失败，并确认整套测试通过。\",\n  \"B\": \"1. 当前函数不修改 `panel:prefs` 或清空 `localStorage`。键不存在、解析失败或解析结果不是普通对象时返回默认值副本。普通对象与默认值合并，已有字段优先，未知字段保留，未校验各字段值。\\n\\n2. 这次没有 schema version 或 migration，也没有安排 migration。`prefs.test.ts` 的 8 个测试通过，整套测试也已全绿，可以合入。\"\n}"
  }
]
